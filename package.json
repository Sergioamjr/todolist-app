{
  "name": "gamified-todo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter todo-list-api-node dev\" \"pnpm --filter todo-list-web dev\"",
    "build": "pnpm --filter todo-list-api-node build && pnpm --filter todo-list-web build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
